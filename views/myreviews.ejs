<%- include("partials/header") %>
<h1>My Reviews</h1>

<% if (locals.reviews.length > 0) { %>
  <% reviews.forEach(r => { %>
    <div>
      <h2><%= r.postTitle %></h2>
      <p><strong><%= r.userName %></strong> — <%= r.formattedDate %></p>
      <p><strong>Book:</strong> <%= r.bookName %> / <%= r.genre %></p>
      <p><%= r.review %></p>
      <a href="/edit?id=<%= r.id %>">Edit</a> <a href="#">Delete</a>
    </div>
    <hr>
  <% }) %>
<% } else { %>
  <p>No reviews yet.</p>
<% } %>

<a href="/">Back to Home</a>
<%- include("partials/footer") %>
